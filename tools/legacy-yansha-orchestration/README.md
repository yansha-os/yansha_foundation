# Legacy Monolithic Orchestration Tooling

> **Status:** INACTIVE HISTORICAL TOOLING
>
> Scripts in this directory were developed for orchestrating multi-agent work streams, test validation, and token quotas within the monolithic Yansha repository.
>
> - Scripts may contain hardcoded path assumptions matching the old monolithic workspace topology.
> - `seed-blob.json` was omitted as it is deterministically generated by `make-seed-blob.ts`.
> - These scripts are **NOT** wired into Foundation package builds or CI/CD pipelines.
